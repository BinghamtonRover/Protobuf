syntax = "proto3";

message AdjustCamera {
	int32 id = 1;
	bool is_enabled = 2;
	int32 resolution = 3;
}

message CameraStatus {
	int32 id = 1;
	bool is_enabled = 2;
	int32 resolution = 3;
}

message VideoData {
	repeated CameraStatus cameras = 1;
}
